import 'package:flutter/material.dart';

class TarjetPage extends StatelessWidget {
  const TarjetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hello TarjetPage'),
      ),
    );
  }
}
